# Integration Tests

These tests are a Bruno proof-of-concept. A dummy test has been added, and all
it does is call the `health` endpoint.

## Running Locally

These tests can be run locally:

- run `npm ci`
- start the backend (`Run and Debug` in Activity Bar > `CSTAR` or
  `CSTAR Backend`)
- run `npm test`
