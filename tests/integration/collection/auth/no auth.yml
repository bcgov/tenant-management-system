info:
  name: no auth
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/roles"
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "401"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
