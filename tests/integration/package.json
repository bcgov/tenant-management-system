{
  "name": "integration-tests",
  "version": "1.0.0",
  "scripts": {
    "test": "cd collection && npx bru run --env default --noproxy"
  },
  "devDependencies": {
    "@usebruno/cli": "^3.2.2"
  }
}
