meta {
  name: Get Roles - No Auth
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/v1/roles
  body: none
  auth: none
}

tests {
  test('status is 401', function () {
    expect(res.getStatus()).to.equal(401)
  })
}
